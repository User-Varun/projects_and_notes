- Enum is a class in java (exception --> u can't extends it , but other things we can constructor , methods)
- Enum are consist of named constants which can be looped over.
    
    ```java
    enum status{
      Start , Running, Pause, Play
    }
    ```
    
- &nbsp;the .ordinal method prints the order of the given object (inside status enum)