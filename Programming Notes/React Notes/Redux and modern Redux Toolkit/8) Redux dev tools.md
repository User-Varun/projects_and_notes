**Setup Redux Dev tools:-**

1.  **install redux-dev-tools extension from chrome store**
2.  **install the npm package `npm i @redux-devtools/extension`**
3.  **that's it when inspecting redux tab option get's added next to components and profiler**
4.  **we can jump back and forth between actions, also debug.**