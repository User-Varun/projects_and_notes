watch the better file structure video from course (section 6 , video 63)  
<br/>1.create separate folder for routes  
<br/>2.create separate folder for controllers ( fat model thin controllers , offload as much as business logic on 'model' as possible )  
<br/>3.create separate folder for models  
<br/>4\. server.js ( mongodb + server related code gets here )  
<br/>5\. config.env file ( environment variable , db , password gets here )  
<br/>6\. set up eslint for better catching bugs and maintain structure