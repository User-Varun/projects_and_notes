# Offline first - sync later system ( APP => local DB => API )

- Requires overhaul of the whole app, how we fetch , good this is i created a hook that sends all data from a central place, so changing there will take effect everywhere