\-Types of SQL commands:  
<br/>1) DDL (data definiton language) commands:  
<br/>\- used to create or chagne or delete any database objects  
create, alter, drop, truncate, rename  
<br/>2) DML ( data manipulation language ) commands  
used to fethch data / enter new data/ changing existed data / deleting the data from table  
<br/>\-insert, update, delete, truncate  
<br/>3) DRL (data retrieval language ) Command  
\-SELECT (logical command )  
<br/>4) DCL (Data control naguage ) Commands  
\- Used to control the access of data base objects. These commands are used by DBA ( Database admisinstrator )  
\- GRANT -REWOKE  
<br/>5) TCL (TRANSCTION CONTROL LANGUAGE ) cOMMANDS  
\- used to save or cancel the actions /transactions made on table data .COMMIT ROLLBACK SAVEPOINT